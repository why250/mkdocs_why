# 个人笔记库协作约定

- 网站源文件是 `docs/`；博客文章按 `docs/blog/posts/{analog,rf,tools,life}/` 归档。已发布 PDF、源码和图片保留现有路径；新文章图片保存到 `docs/blog/posts/image/<article-slug>/`。
- 仅在用户明确调用 `$analog-ic-notes` 或要求“整理成笔记”时创建新笔记。按类别选择文章目录，文件名使用简短的英文 `kebab-case` 技术 slug，且不得覆盖任一文章目录下的已有文件。
- 新笔记默认 `draft: true`。生成前阅读相邻文章以沿用元数据、分类、标签和写作风格；除非用户明确要求，不修改旧文章。
- LLM 可以生成和校验本地草稿，但不得提交、推送、发布，或将草稿改为可发布状态。用户审核后自行完成这些操作。
